apply plugin: "org.springframework.boot"
apply plugin: "org.graalvm.buildtools.native"

dependencies {
}

graalvmNative {
    binaries {
        main {
            sharedLibrary = false
        }
    }
}
